# Just another Snake Game in C

Um programa de terminal (TUI) feito em C com ncurses. Um classico joguindo da cobra

## Uso

Dependências: `gcc`, `make` e a biblioteca `ncurses`

```bash
  make run
```

---

![App Screenshot](./preview.GIF)
